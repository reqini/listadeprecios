# Supabase (Fase 1 — Auth + roles)
# Creá el proyecto en https://supabase.com y copiá estos valores desde
# Project Settings > API. NUNCA pegues los valores reales acá ni en el chat —
# completalos solo en tu .env.local (ya está en .gitignore).
REACT_APP_SUPABASE_URL=
REACT_APP_SUPABASE_ANON_KEY=

# Backend actual (legacy) — se mantiene mientras dure la migración por fases.
REACT_APP_API_BASE=
